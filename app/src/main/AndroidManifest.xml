<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jannikokan.timetable">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/app_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/app_icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Table.HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Helper.BottomNavVieweHerlper" />
        <activity android:name=".Helper.DatabaseHelper" />
        <activity android:name=".Note.NoteActivity" />
        <activity android:name=".Calender.CalenderActivity" />
        <activity android:name=".Edit.EditActivity" />
        <activity android:name=".Table.StundeZuweisen" />
        <activity android:name=".Edit.ZeigeFaecherListe" />
        <activity android:name=".Edit.ZeigeLehrerListe" />
        <activity android:name=".Edit.ZeigeRaumListe" />
    </application>

</manifest>